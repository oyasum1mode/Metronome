# Metronome Pro 取扱説明書

マーチング・吹奏楽の練習向けに作られた、ちょっと賢いメトロノームです。
普通のメトロノームとして使うのはもちろん、曲を 1 曲まるごと登録しておけば、テンポチェンジや拍子チェンジも自動で切り替えながら最後まで通して鳴らしてくれます。
クリック音は中低域を少し厚めに調整してあるので、Bluetooth スピーカーでも芯のある音で鳴ります。

**公開 URL**: https://oyasum1mode.github.io/Metronome/

---

## もくじ

- [動作環境](#動作環境)
- [ホーム画面に追加する（推奨）](#ホーム画面に追加する推奨)
- [クイックスタート](#クイックスタート)
  - [① 普通のメトロノームとして使う](#-普通のメトロノームとして使う)
  - [② 曲を登録して練習する](#-曲を登録して練習する)
- [画面構成（4 タブ）](#画面構成4-タブ)
- [機能リファレンス](#機能リファレンス)
  - [拍子（共通）](#拍子共通)
  - [テンポタブ](#テンポタブ)
  - [パフォーマンスタブ](#パフォーマンスタブ)
  - [曲編集タブ](#曲編集タブ)
  - [ライブラリタブ](#ライブラリタブ)
- [共通機能](#共通機能)
- [データの保存と共有](#データの保存と共有)
- [Bluetooth スピーカーを使うときの注意](#bluetooth-スピーカーを使うときの注意)
- [よくある質問・困ったとき](#よくある質問困ったとき)

---

## 動作環境

| 環境 | 対応 | 備考 |
|------|------|------|
| iPhone（Safari） | ◯ | ホーム画面追加に対応 |
| iPhone（Chrome） | △ | 動きますがホーム画面追加は不可 |
| Android（Chrome） | ◯ | ホーム画面追加に対応 |
| パソコン（各種ブラウザ） | ◯ | スペースキーなどショートカットあり |

- インターネット接続がなくても動きます（一度開いたあとはオフライン可）
- 縦向きスマートフォンでの片手操作を想定したデザインです

---

## ホーム画面に追加する（推奨）

普通のアプリのように使えるようになります。

### iPhone（Safari）

1. 公開 URL を Safari で開く
2. 画面下の共有ボタン（□に↑のアイコン）をタップ
3. 「ホーム画面に追加」を選ぶ

> **iPhone Chrome では追加できません。** Safari で開き直してください。

### Android（Chrome）

1. 公開 URL を Chrome で開く
2. 右上のメニュー（︙）→「ホーム画面に追加」

---

## クイックスタート

### ① 普通のメトロノームとして使う

「テンポタブ」を開くだけで、すぐに普通のメトロノームとして使えます。

1. 画面下の **テンポ** タブを選ぶ
2. BPM スライダー、または **±1 / ±5 / ±10** ボタンでテンポを設定
3. **拍子** ドロップダウンで拍子を選ぶ（4/4、6/8、12/8 など 12 種から選択）
4. **Start** ボタンで再生 / もう一度押すと停止

**Tap Tempo**（タップでテンポを取る）
- スタートボタンの左にある **Tap** ボタンを、合わせたいテンポで何度かタップすると BPM が自動で算出されます

> ヒント：パソコンならスペースキーで再生 / 停止、↑↓ キーで BPM ±1 ができます。

---

### ② 曲を登録して練習する

「曲編集」で曲を作り → 「ライブラリ」に保存 → 「パフォーマンス」で再生、という流れです。

#### Step 1. 曲を作る（曲編集タブ）

1. **曲名** を入力
2. **+ セクション追加** で必要な数だけセクションを足す
   - セクションは `A`、`B`、`C`…と自動でラベルが付きます
   - 各セクションに **開始小節 / 小節数 / テンポ / 拍子** を入力
   - 名前（例：「Intro」「Bridge」など）も付けられます
3. 必要なら **+ エンドブロック** を追加（曲の最後の "ジャン" 用、頭 1 拍だけ鳴る）
4. **💾 ライブラリに保存** をタップ

#### Step 2. 練習する（パフォーマンスタブ）

1. **ライブラリ** タブで保存した曲をタップ → 自動でパフォーマンスタブに切り替わります
2. 必要に応じて **Tap Off**（カウントイン）、**End Check**（終了確認音）、**練習範囲**、**テンポオフセット** を設定
3. **Start** ボタンで再生

セクションが切り替わるたびに、テンポも拍子も自動で変わります。

---

## 画面構成（4 タブ）

画面下のタブで切り替えます。

| タブ | 役割 |
|------|------|
| **テンポ** | 単独メトロノーム。曲とは無関係 |
| **パフォーマンス** | 登録した曲を通して再生する練習用ページ |
| **曲編集** | 曲（セクション）を作る・編集する |
| **ライブラリ** | 保存した曲の一覧。読み込み・書き出し・削除 |

---

## 機能リファレンス

### 拍子（共通）

テンポタブ・曲編集タブの両方で、以下 12 種から拍子を選べます。

| 表示 | 想定用途 |
|------|---------|
| `1` | 全拍アクセント（テスト用） |
| `2/4` 〜 `8/4` | 通常の単純拍子 |
| `9/4` | 9 拍の単純拍子（1 拍目のみアクセント） |
| `6/8` / `9/8` / `12/8` | 複合拍子（8 分音符基準） |

**BPM の基準について：**
- 4 系拍子（`4/4`、`9/4` など）：BPM = **4 分音符** が 1 分間に何回鳴るか
- 8 系拍子（`6/8`、`9/8`、`12/8`）：BPM = **8 分音符** が 1 分間に何回鳴るか

つまり `6/8` で BPM 120 を選ぶと、8 分音符が 1 分間に 120 個鳴ります（1 小節 = 3 秒）。楽譜上の指定どおりの感覚で BPM を入れて大丈夫です。

> `12/8` + 16th Subdivision のように細かく設定すると画面のドットが多くなり、スマホの狭い画面では 2 〜 3 段に折り返して見えることがあります。

---

### テンポタブ

単独のメトロノームです。曲とは完全に独立しています。

| 操作 | 説明 |
|------|------|
| BPM スライダー | テンポ（20〜400）を変更 |
| ±1 / ±5 / ±10 | BPM を細かく / 大きく増減 |
| 拍子セレクト | 12 種から選択（4/4 など、上の「拍子（共通）」参照） |
| Tap | タップしたリズムから BPM を自動算出 |
| Subdivision | OFF / 8th / Tri / 16th の 4 ボタン。再生中も切替可能 |
| Start / Stop | 再生 / 停止（スペースキー可） |

---

### パフォーマンスタブ

登録した曲を通して再生する、練習用のメインページです。

#### 練習範囲

`開始セクション → 終了セクション` をドロップダウンで指定すると、その範囲だけを繰り返し練習できます。

例：「B から D まで通したい」というときに使います。

#### Tap Off（カウントイン）

再生開始前に **「ワン・ツー・スリー・フォー」** とカウント音を鳴らしてから本編を始める機能です。

- **OFF / 4 拍 / 8 拍** を選択
- カウント中はビートドットが紫色になります

#### End Check（終了確認ビート）

練習範囲の最後に、終わりを知らせる確認ビートを鳴らす機能です。
吹いている / 演奏している側が「ちゃんと最後まで合ってたか」を耳で確認できます。

- **OFF / 4 拍 / 8 拍** を選択
- **前テンポ** … 直前のセクションのテンポで鳴らす
- **後テンポ** … 次のテンポで鳴らす（次に繋げる練習に便利）
- 確認ビート中はビートドットが緑色になります

#### Subdivision（裏打ち音）

ON / OFF の 2 ボタン。ON にすると、曲編集で各セクションに設定した裏打ち（OFF / 8th / Tri / 16th）が自動で鳴ります。OFF なら全セクションで裏打ちなし。

セクションが切り替わるたびに鳴り方が自動で変わります。例：「A は 8th で刻みたい、B では 16th で細かく確認したい」というセクションごとの細かい設定が再生時に勝手に切り替わります。

#### テンポオフセット（遅練習用）

曲全体のテンポを **一括で** ずらせます。難しい部分をゆっくり通したいときに便利です。

- **−10 / In tempo / +10** ボタン
- たとえば `+30` を押すと、曲中のすべてのセクションが BPM +30 で鳴ります
- 元に戻すには **In tempo** をタップ
- ボタン上に現在のオフセット値が表示されます（例：`In tempo (+30)`）
- リロード、別の曲を読み込み直したときは自動で 0 に戻ります

#### accel / rit のセクション

曲編集で「テンポ変化あり」に設定したセクションを再生中は、

- BPM 数値が拍ごとに滑らかに変わる
- テンポ名の隣に **↗（accel）** または **↘（rit）** が出る

---

### 曲編集タブ

#### セクション

通常のセクション。曲の途中の各まとまりを表します。

| 項目 | 説明 |
|------|------|
| 名前 | 任意（例：Intro、Verse、Chorus） |
| ラベル | A、B、C… と自動で付く |
| 開始小節 | 曲中の何小節目から始まるか |
| 小節数 | このセクションが何小節続くか（**0 を入れると手動進行モード**） |
| テンポ | このセクションの BPM |
| 拍子 | 12 種から選択（4/4、6/8 など。上の「拍子（共通）」参照） |
| ▲ ▼ | セクションの順序を入れ替える |

##### 詳細：テンポ変化と Subdivision

セクションの **「⋯ 詳細」** を開くと、テンポ変化（accel / rit）とセクション専用の裏打ち音を設定できます。

- **終了テンポ**：セクションの最後の BPM。開始テンポと違う値を入れれば accel（だんだん速く）/ rit（だんだん遅く）になります
- **カーブ**：変化のかかり方
  - 中央：均等に変化（線形）
  - 左：前半でぐっと変わる
  - 右：後半でぐっと変わる
- **Subdivision**：このセクション専用の裏打ち音（OFF / 8th / Tri / 16th）。パフォーマンス再生時、画面側の Subdivision が ON ならこの設定で鳴ります

#### エンドブロック

曲の終わりに「ジャン」と最後の音を入れたいときに使う、特殊なブロックです。

- 1 曲に 1 つだけ
- 必ず曲の最後に固定（▲▼ で動かせません）
- 頭 1 拍だけ鳴ります（最後の打点として）
- ビートドットが黄色

#### エクスポート / インポート

曲データを **Base64 形式の文字列** として書き出し / 読み込みできます。

- 別のスマホやパソコンに曲を移したいときはこれをコピー＆ペーストで送ります
- LINE やメールで共有可能

#### クリア

編集中の内容をすべて消します（ライブラリの曲は消えません）。

---

### ライブラリタブ

保存した曲の一覧画面です。

- 曲名をタップ → パフォーマンスタブに自動で切り替わる
- 各曲から **エクスポート** や **削除** ができる
- 一番下の **+ 新しい曲を作成** で曲編集タブへ
- データは **このブラウザの中にだけ** 保存されます（端末を変えるならエクスポート / インポートで）

---

## 共通機能

### Subdivision Click（裏打ち音）

メインビートのあいだに細かい裏打ち音を追加する機能です。タブごとに操作が異なります。

#### テンポタブ
**OFF / 8th / Tri / 16th** の 4 ボタン。タップで切替、再生中もリアルタイムで切り替わります。

#### パフォーマンスタブ
**ON / OFF** の 2 ボタン。ON にすると、曲編集で各セクションに設定した裏打ちで鳴ります（セクションが OFF 設定なら無音）。

#### 曲編集タブ
セクションの **「⋯ 詳細」** 内で OFF / 8th / Tri / 16th を選びます。セクションごとに違う裏打ち（例：A は 8th、B は Tri）を設定できます。

#### 鳴り方の一覧

| 表示 | 鳴り方 | 使いどころ |
|------|--------|-----------|
| `OFF` | 裏打ちなし | 通常 |
| `8th` | 8 分音符（1 拍を 2 分割） | 8 ビート系 |
| `Tri` | 3 連符（1 拍を 3 分割） | 3 連 / シャッフル |
| `16th` | 16 分音符（1 拍を 4 分割） | 細かい刻みを意識したいとき |

- 裏打ちはメインビートとは違う音色で、少し小さい音量で鳴ります
- メインのドットの間に、小さい補助ドットが表示されます
- カウントイン中・終了確認ビート中は裏打ちは鳴りません

### マスター音量

画面上部の音量スライダーで全体の音量を調整できます。設定はブラウザに保存されます。

### ショートカットキー（パソコン用）

| キー | 動作 |
|------|------|
| スペース | 再生 / 停止（テンポ・パフォーマンスタブ） |
| ↑ | BPM +1（テンポタブ） |
| ↓ | BPM −1（テンポタブ） |

### 画面スリープ防止

再生中はスマホの画面が自動で暗くなりません（対応端末のみ）。停止すると通常通りスリープします。

---

## データの保存と共有

- 曲とマスター音量はブラウザの中（`localStorage`）に保存されます
- **ブラウザのデータを削除すると曲も消えます**。大事な曲は **エクスポート** でバックアップを取ってください
- 別の端末・別のブラウザに曲を移したい場合：
  1. 元の端末でその曲の **エクスポート** をタップ
  2. 出てきた長い文字列（Base64）を LINE やメモにコピー
  3. 移したい端末で曲編集タブを開き、**インポート** にその文字列を貼り付け

---

## Bluetooth スピーカーを使うときの注意

Bluetooth スピーカーで音を出すと、有線と比べて **100〜400 ミリ秒ほど音が遅れます**。これは Bluetooth の規格上の遅延で、アプリ側では取り除けません。

- **メインビートとサブビートの相対的なタイミングは正確** に保たれているので、練習用としては問題なく使えます
- どうしても遅延が気になる場合は、有線イヤホンや有線スピーカーを使ってください

---

## よくある質問・困ったとき

**Q. 音が鳴らない**
- マスター音量がゼロになっていないか確認してください
- iPhone は本体のサイレントスイッチがオンだと音が出ない場合があります（ブラウザによる）
- 別のブラウザで開き直すと直ることがあります

**Q. 保存したはずの曲が消えた**
- ブラウザの「履歴 / サイトデータの削除」をすると、保存した曲も一緒に消えます
- 大事な曲は **エクスポート** でバックアップを取ってください

**Q. 拍と画面のドットが微妙にズレて見える**
- 音は正確に鳴っていますが、Bluetooth スピーカーでは音だけ遅れて到着するため、画面と音がズレて感じることがあります（音側のみ遅延）

**Q. iPhone Chrome でホーム画面に追加できない**
- iOS では Safari からのみ追加できます。Safari で開き直してください

**Q. 仲間に曲データを送りたい**
- 曲編集タブまたはライブラリタブの **エクスポート** で出てきた文字列を送ってもらい、相手はインポートで貼り付ければ復元できます

---

困ったときや要望があれば作者まで連絡してください。
Instagram: _doddy